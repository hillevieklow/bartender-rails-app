<div class="row outlined-section">
  <h1>Recipes containing <%= @ingredient.name %>:</h1>
  <% for r in @ingredient.recipes %>
    <h3><%= link_to r.name, recipe_path(r) %></h3>
    <p><%= r.description %></p>
  <% end %>
</div>
